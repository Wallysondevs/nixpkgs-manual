# scons {#scons}

Sobrescreve as fases de build, install e check. Isso usa o sistema de build scons como um substituto para make. scons não fornece uma fase de configure, então tudo é gerenciado no tempo de build e install.