# bmake {#bmake-hook}

[bmake](https://www.crufty.net/help/sjg/bmake.html) é a variante portátil do utilitário `make` do NetBSD.

No Nixpkgs, o `bmake` vem com um hook que sobrescreve as fases padrão de build, check, install e dist.