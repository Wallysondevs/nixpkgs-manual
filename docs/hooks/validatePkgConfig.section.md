# validatePkgConfig {#validatepkgconfig}

O hook `validatePkgConfig` valida todos os arquivos pkg-config (`.pc`) em um pacote. Isso ajuda a identificar alguns erros comuns em arquivos pkg-config, como variáveis indefinidas.