# gdk-pixbuf {#setup-hook-gdk-pixbuf}

Exporta a variável de ambiente `GDK_PIXBUF_MODULE_FILE` para o *builder*. Adicione o pacote `librsvg` a `buildInputs` para obter suporte a SVG. Veja também a [descrição do *setup hook* na documentação da plataforma GNOME](#ssec-gnome-hooks-gdk-pixbuf).