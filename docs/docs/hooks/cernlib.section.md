# CERNLIB {#cernlib-hook}

Este hook define as variáveis de ambiente `CERN`, `CERN_LEVEL` e `CERN_ROOT`. Elas fazem parte do [sistema de build do CERNLIB](https://cernlib.web.cern.ch/install/install.html) e são necessárias para que alguns programas compilem corretamente.